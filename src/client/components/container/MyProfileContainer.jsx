import { connect } from "react-redux";
import MyProfile from "../pure/MyProfile/MyProfile";

const mapStateToProps = (state) => {
    return {}
};

export default connect(
    mapStateToProps
)(MyProfile);